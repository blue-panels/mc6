[global]
   workgroup = WORKGROUP
   server string = stream sandbox
   server role = standalone server
   security = user
   map to guest = never
   disable netbios = yes
   smb ports = 445
   log level = 1
   log file = /var/log/samba/log.%m

[cases]
   path = /home/mc/cases
   valid users = mc
   read only = yes
   browseable = yes
